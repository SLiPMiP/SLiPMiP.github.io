let object = []
object.push(rect(30, 30, 30, 30),line(0, 0, windowWidth, windowHeight))

function setup() {
    createCanvas(windowWidth, windowHeight);
}

function draw() {
    background(120)
object.draw[1]
    
}